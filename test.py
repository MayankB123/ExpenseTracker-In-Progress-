# import sqlite3
# #
# con = sqlite3.connect("et_database.db")
# #
# cursor = con.cursor()
# #
# cursor.execute("DELETE FROM income_table WHERE Email='mayankbajpaix@gmail.com'")
# #
# con.commit()

# cursor.execute("create table expense_table (Email varchar(50), eName varchar(50), eDate varchar(50), Category varchar(50), Amount decimal)")
#
# con.commit()